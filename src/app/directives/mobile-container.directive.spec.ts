import { MobileContainerDirective } from './mobile-container.directive';

describe('MobileContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileContainerDirective();
    expect(directive).toBeTruthy();
  });
});
